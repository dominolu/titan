# Titan 项目执行环境约定

本文件适用于整个仓库。除非用户在当前任务中明确给出不同指示，所有开发和验证工作必须遵守以下环境分工。

## 本地开发机

- 本地仅用于编写、审查和修改代码与文档。
- 不在本地执行项目编译、依赖构建、代码生成、格式化、静态检查、单元测试、集成测试或实盘测试。
- 本地修改完成后，将需要验证的代码同步到编译服务器，再执行构建和测试。

## 编译服务器

- 固定使用 `192.168.3.88` 作为项目编译与非实盘验证服务器。
- 项目编译、依赖构建、代码生成、格式检查、静态检查、单元测试和不接入实盘账户的集成测试均在该服务器执行。
- 构建产物应从该服务器产生；不要使用本地构建产物进行部署或实盘验证。
- 在执行命令前先确认远端仓库路径、代码版本和工作区状态，避免覆盖服务器上的未提交修改。

## 远程实盘服务器

- 连接真实交易所账户、读取实盘账户状态、验证实盘下单/撤单以及策略实盘运行，只能在远程实盘服务器执行。
- 远程实盘服务器不承担日常项目编译；部署到该服务器的程序必须使用编译服务器生成并验证过的构建产物。
- 实盘测试前必须确认目标账户、环境、交易品种、数量、价格保护和最大风险敞口；优先采用最小数量、可立即撤销的测试订单。
- 未经用户明确授权，不启动长期运行的实盘策略，不扩大仓位，不转账，也不修改账户级交易模式。

## 标准流程

1. 在本地编写或修改代码和文档。
2. 将代码同步到 `192.168.3.88`，在该服务器完成编译、静态检查和非实盘测试。
3. 仅将验证通过的构建产物部署到远程实盘服务器。
4. 在远程实盘服务器执行受控的实盘账户检查和最小风险下单/撤单测试。
5. 记录代码版本、构建结果、部署版本和实盘测试结果；任何阶段失败都不得继续进入下一阶段。
